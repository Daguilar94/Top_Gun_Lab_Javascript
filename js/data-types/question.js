// STRING
// 1. Encuentra la longitud de la siguiente variable:
var texto = "Espero que el refrigerio esté rico esta vez";
console.log(texto);

// 2. concatena las variables nombre y apellido separadas por un espacion en blanco 
// y asígna este nuevo valor a una variable llamada nombreCompleto.

var nombre = "Brendan";
var apellido= "Eich";



// 3. repetir el caracter 'x' 50 veces y asignar este valor a una variable llamada repetido



// NUMEROS
// 1. Enuentra la suma de numOne y numTwo y asigna este valor a una variable llamada suma

var numOne = 6;
var numTwo = 10


// 2. Utilizando la variable suma, creada en el ejercicio anterior, encuentra el valor resultante de dividir suma entre la variable dividendo y asignalo a una variable llamada cociente.

var dividendo = 4;

// 3. Utiliza la variable cociente y encuentra el resultado de elevar este valor al valor de la variable exponente y asignalo a una variable llamada potencia.

var exponente = 3

// 4. Encuentra el valor de la raíz cuadrada de la variable potencia y asígnala a una variable llamada raiz



// ARREGLOS
// 1. Encuentra la longitud del siguiente arreglo:
var primerArreglo = [323, "Hola", [1, 2]];

// 2. Encuentra el elemento situado en la segunda posición del arreglo primerArreglo.

// 3. Elimina la última posición del arreglo primerArreglo y asígnalo a una variable llamada subArray.

// 4. Agrega el valor de subArray a la primera posición del arreglo
// primerArreglo (que ya está modificado) sin eliminar el valor contenido
// que está en la primera posición actual.


// OBJETOS
// 1. crea un objeto que contenga las llaves 'nombre' y 'apellido'
// y valores 'Brendan' y 'Eich' respectivamente y asígnalo a una variable llamada fundador.

// 2. imprime en consola el valor de la llave apellido del objeto fundador.

//3. agrega una llave adicional al objeto fundador llamada info cuyo valor sea la variable 

//4. imprime en consola el valor 'hola' contenido en el valor de la llave info del objeto fundador.




